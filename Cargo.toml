[package]
name = "foobot2"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
dotenv = "0.15"

diesel = { git = "https://github.com/diesel-rs/diesel.git", features = ["mysql", "r2d2"] }
diesel_migrations = { git = "https://github.com/diesel-rs/diesel.git" }

tokio = { version = "1.6", features = ["full"] }
async-trait = "0.1"
#async-recursion = "0.3"

tracing = "0.1"
tracing-subscriber = "0.2"

reqwest = { version = "0.11", default-features = false, features = ["json", "rustls-tls"] }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

twitch-irc = { git = "https://github.com/robotty/twitch-irc-rs.git", default-features = false, features = ["transport-tcp-rustls-webpki-roots"] }
serenity = { version = "0.10", default-features = false, features = ["client", "gateway", "rustls_backend", "model", "cache"] }

handlebars = { git = "https://github.com/sunng87/handlebars-rust.git" }

rocket = {git = "https://github.com/SergioBenitez/Rocket.git", features = ["secrets"] }
rocket_dyn_templates = { git = "https://github.com/SergioBenitez/Rocket.git", features = ["handlebars"] }

passwords = "3.1"

anyhow = "1.0"